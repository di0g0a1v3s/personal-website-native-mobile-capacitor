import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diogomartinsalves.app',
  appName: 'Diogo Alves Personal Page',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
